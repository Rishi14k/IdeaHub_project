// const express = require('express');
// const { sendRequest, acceptRequest, rejectRequest } = require('../controllers/collaborateController');
// const verifyToken = require('../middleware/authMiddleware');

// const route = express.Router();


// route.post('/sendRequest/:id', verifyToken,sendRequest);
// route.post('/acceptRequest/:id', verifyToken,acceptRequest);
// route.post('/rejectRequest/:id', verifyToken,rejectRequest);

// module.exports = route;